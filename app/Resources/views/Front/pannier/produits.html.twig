{% extends ':Front:base.html.twig' %}
{% block title %}Ma liste de course{% endblock %}
{% block description %}Ma liste des produits souhaités{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="content col-md-8">

                {% for produit in maListe %}
                    <div class="coupon-list list-wrapper">
                        <div class="coupon-wrapper">
                            <div class="row">
                                <div class="col-md-4 col-sm-4 col-xs-12">
                                    <div class="post-media text-center">
                                        <a href="javascript:void(0)"><img src="{{ asset('uploads/produits/' ~ produit.produit.url) }}" alt="" class="img-responsive" style="width: 209px;height: 162px"></a>
                                        {#<small><a href="javascript:void(0)">{{ produit.produit.marque }}</a></small>#}
                                    </div>
                                </div>
                                <div class="col-md-8 col-sm-8 col-xs-12">
                                    <div class="coupon-meta">
                                        <div class="coupon-top clearfix">
                                            <div class="favorite-coupon pull-right text-right">
                                                <a href="{{ path('front_pannier_delete_produit',{id:produit.id}) }}" data-toggle="tooltip" data-placement="bottom" title="Supprimer"><i class="fa fa-trash"></i></a>
                                            </div>
                                        </div><!-- end coupon-top -->
                                        <h3><a href="javascript:void(0)">{{ produit.produit.libelle }} x {{ produit.quantity }}</a></h3>
                                        <p>{{ produit.produit.description }}</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
                <div class="coupon-wrapper pull-right col-md-4 col-sm-4 col-xs-12">
                {% if maListe|length>0 %}
                    <a target="_blank" href="{{ path('front_pannier_pdf_produit') }}" class="show-code print-code"><span class="fa fa-print"></span> Imprimer Ma liste </a>
                {% endif %}
                    </div>
            </div>
            <div class="sidebar col-md-4 col-sm-12">
                {{ render(controller("frontGeneralBundle:Supermarche:populaire")) }}

            </div>
        </div>
    </div>
{% endblock %}

