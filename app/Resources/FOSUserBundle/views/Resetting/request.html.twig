{% extends ":Back:fosbase.html.twig" %}

{% block body %}
<div class="box-forgot" style="display: block;">
    <h3>Mot de passe oublié?</h3>
    <p>
       Enter votre adresse mail.
    </p>
    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="form-forgot">
        <div>
            {% if invalid_username is defined %}
                <div class="errorHandler alert alert-danger no-display">
                    <i class="fa fa-remove-sign"></i> You have some form errors. Please check below.
                </div>
            {% endif %}
            <fieldset>
                <div class="form-group">
							<span class="input-icon">
								<input type="text" id="username" class="form-control" name="username" placeholder="Email" required="required">
								<i class="fa fa-envelope"></i> </span>
                </div>
                <div class="form-actions">
                    <a class="btn btn-light-grey go-back" href="{{ path('fos_user_security_login') }}">
                        <i class="fa fa-circle-arrow-left"></i> Retour
                    </a>
                    <button type="submit" class="btn btn-bricky pull-right">
                        Valider <i class="fa fa-arrow-circle-right"></i>
                    </button>
                </div>
            </fieldset>
        </div>

    </form>
</div>
{% endblock %}
