{% extends ":Back:base.html.twig" %}
{% block title %}Gestion des clients{% endblock %}
{% block description %}{% endblock %}
{% block topbody %}
    {{ parent() }}
    <li>
        <a href="javascript:void(0)" >Gestion des clients</a>
    </li>
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-3 col-md-offset-9">
            <a class="btn btn-primary right" href="{{ path('back_general_client_ajouter') }}"><i class="fa fa-plus"></i>
                Ajouter client</a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <table class="table table-striped table-bordered table-hover table-full-width table-condensed table-hover"  id="sample_1">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th>Username</th>
                            <th>Email</th>
                            <th>Nom</th>
                            <th>prénom</th>
                            <th class="center" >Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for client in clients %}
                            <tr>
                                <td>{{ client.id }}</td>
                                <td>{{ client.username }}</td>
                                <td>{{ client.email }}</td>
                                <td>{{ client.nom }}</td>
                                <td>{{ client.prenom}}</td>
                                <td class="TableAction">
                                    <div class="visible-md visible-lg hidden-sm hidden-xs">
                                        {{ macros.tableEdit(path('back_general_client_modifier',{'id':client.id})) }}
                                        {{ macros.tableDelete(path('back_general_client_delete',{'id':client.id})) }}
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}