{% extends ":Back:base.html.twig" %}
{% block title %}Gestion des Marques{% endblock %}
{% block description %}{% endblock %}
{% block topbody %}
    {{ parent() }}
    <li>
        <a href="javascript:void(0)" >Gestion des Marques</a>
    </li>
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-2 col-md-offset-10 btn_ajout">
            <a class="btn btn-primary right " href="{{ path('back_general_marque_ajouter') }}"><i class="fa fa-plus"></i>
                Ajouter marque</a>
        </div>
    </div>
    <div class="clear"></div>
    <div class="row">
        <div class="col-md-12">

            <div class="panel panel-default">
                <div class="panel-body">
                    <table class="table table-striped table-bordered table-hover table-full-width table-condensed table-hover"  id="sample_1">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th>Logo</th>
                            <th>Libelle</th>

                            <th class="center" >Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for marque in marques %}
                            <tr>
                                <td>{{ marque.id }}</td>
                                <td> <img src="{{ asset('uploads/marques/' ~ marque.url) }}" style="max-width:50px;"></td>
                                <td>{{ marque.libelle }}</td>
                                <td class="TableAction">
                                    <div class="visible-md visible-lg hidden-sm hidden-xs">
                                        {{ macros.tableEdit(path('back_general_marque_modifier',{'id':marque.id})) }}
                                        {{ macros.tableDelete(path('back_general_marque_delete',{'id':marque.id})) }}
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}